/**
 * Adds two numbers together, returning the sum.
 */
export function makeHappyText(happy) {
  return happy ? 'I am very happy!' : 'No I am sad. :(';
}

export function makeSadText(sad) {
  return sad ? 'I am very sad!' : 'No I am happy! :)';
}
